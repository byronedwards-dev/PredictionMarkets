[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run worker:start"
# No healthcheck - this is a background worker, not a web server
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
